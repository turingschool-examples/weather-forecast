const receiveLocation = (state={}, action) => {
  switch (action.type){
    case 'RECIEVE_LOCATION':
    debugger
      return action
    default:
      return state
  }
}

export default receiveLocation
